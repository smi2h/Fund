<Window x:Class="Fund.Wpf.UI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:views="clr-namespace:Fund.Wpf.UI.Views"
        Title="Funds"
        Width="900"
        Height="800"   >
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="600" />
            <ColumnDefinition Width="250" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="500" />
        </Grid.RowDefinitions>
        <views:CreateStockView Margin="10" />
        <views:StockGridView Margin="10" Grid.Row="1" />
        <views:SummaryInformationView Margin="10" Grid.Row="1" Grid.Column="1" />
    </Grid>
</Window>